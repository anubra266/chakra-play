// import { parse } from "@babel/parser";
// import traverse from "@babel/traverse";
// import MonacoJSXHighlighter from "monaco-jsx-highlighter";

export const highlightJSX = (monaco: any, editor: any) => {
//   const monacoJSXHighlighter = instatiateJSXHighlighter(monaco, editor);

//   const DEBOUNCE_TIME_IN_MILLISECONDS = 100;
//   monacoJSXHighlighter.highLightOnDidChangeModelContent(
//     DEBOUNCE_TIME_IN_MILLISECONDS
//   );

//   monacoJSXHighlighter.addJSXCommentCommand();
};

// const instatiateJSXHighlighter = (monaco: any, editor: any) =>
//   new MonacoJSXHighlighter(monaco, jsxBabelSetup, traverse, editor);

// const jsxBabelSetup = (code: any) =>
//   parse(code, {
//     sourceType: "module",
//     plugins: ["jsx"],
//   });
